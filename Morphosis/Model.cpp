#include "stdafx.h"
//#include "Model.h"
//#include "Mesh.h"
//#include "Texture.h"
//
//void CModel::Render(ID3D12GraphicsCommandList *pd3dCommandList)
//{
//	if (-1 != m_TextureIdx) g_vecTexture[m_TextureIdx]->UpdateShaderVariables(pd3dCommandList);
//	if (-1 != m_MeshIdx)	g_vecMesh[m_MeshIdx]->Render(pd3dCommandList);
//}
//
//void CModel::SetMesh(const char * name)
//{
//	for (int i = 0; i < g_vecMesh.size(); ++i) 
//		if (g_vecMesh[i]->name == name) m_MeshIdx = i;
//}
//
//void CModel::SetTexture(const char * name)
//{
//	for (int i = 0; i < g_vecTexture.size(); ++i)
//		if (g_vecTexture[i]->m_strName == name) m_TextureIdx = i;
//}
